en:
  activerecord:
    models:
      ssh: SSH configuration
    attributes:
      ssh:
        authorized_key: Authorized key
        authorized_keys: Authorized keys
  ssh:
    show:
      no_authorized_keys: "No authorized keys"
    edit:
      submit: Save
      cancel: cancel
fr:
  activerecord:
    models:
      ssh: Configuration SSH
    attributes:
      ssh:
        authorized_key: Clé autorisée
        authorized_keys: Clés autorisées
  ssh:
    show:
      no_authorized_keys: "Pas de clés autorisée"
    edit:
      submit: Modifier
      cancel: retour
